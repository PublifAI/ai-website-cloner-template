# Directory where client data is stored (discovery, assets, reports, output).
# Default: ../clients (parent-level clients folder, shared across all Publifai tools)
# Use absolute path or relative to this repo's root.
CLIENTS_DIR=../clients
